/*!
    @import /amethyst_input/src/bindings.rs#Bindings, StringBindings
    Bindings<StringBindings>
*/

(
    axes: {
        "camera_right": Emulated(
            pos: Key(D),
            neg: Key(A),
        ),
        "camera_up": Emulated(
            pos: Key(W),
            neg: Key(S),
        ),
    },
    actions: {

    },
)

